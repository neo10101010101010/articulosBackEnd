server.port: 8090
spring.mvc.hiddenmethod.filter.enabled: true
spring:

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/articulos
    username: neo
    password: neo
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update